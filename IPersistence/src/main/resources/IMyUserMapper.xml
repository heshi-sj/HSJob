<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lagou.mapper.IMyUserMapper">

    <insert id="insertUser" parameterType="com.lagou.pojo.User">
      insert into user(id,username) values(#{id},#{username})
    </insert>

    <select id="selectById" parameterType="com.lagou.pojo.User" resultType="com.lagou.pojo.User">
      select * from user where id=#{id}
    </select>

    <update id="updateById" parameterType="com.lagou.pojo.User">
        update user set username=#{username} where id=#{id}
    </update>

</mapper>